/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["https://images.ctfassets.net","images.ctfassets.net","pokeapi.co","raw.githubusercontent.com"]
    }
};

export default nextConfig;
